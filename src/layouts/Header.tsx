import React from 'react';

import NavigationBar from '@/components/NavigationBar';

const Header = () => {
  return (
    <div className="mb-16 mt-14 flex w-full flex-col items-center justify-center">
      <NavigationBar />
    </div>
  );
};

export default Header;
