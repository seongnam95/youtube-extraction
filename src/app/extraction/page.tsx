import Input from '@/components/Input';

interface ExtractionPageProps {}

const ExtractionPage = ({}: ExtractionPageProps) => {
  return (
    <div>
      <h1>Extraction</h1>
      <Input />
    </div>
  );
};

export default ExtractionPage;
